// Routes that aren't project-scoped, so they stay reachable with no project selected (or none at
// all yet) and don't show project-related chrome (the switcher, the docs panel/toggle).
export const PROJECT_EXEMPT_ROUTES = ["/account"];
